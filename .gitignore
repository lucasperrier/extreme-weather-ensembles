__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.ipynb_checkpoints/

# Data and outputs live on /workspace, never in the repo.
data/
evalstore/
modelstore/
*.nc
*.tmp-*

# paper/ build artefacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.pdf

# Symlink to $MODEL_ROOT, needed because the model config uses relative
# "modelstore/..." paths. Created per-pod, not tracked.
modelstore
thresholds.log
